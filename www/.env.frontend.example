# URL pública da API Laravel, sem barra final.
# Use /api/v1 quando front-end e API estiverem no mesmo domínio.
VITE_API_URL=/api/v1

# Somente endereços públicos. Nunca adicione seed phrase ou chave privada.
VITE_SOLANA_NETWORK=devnet
VITE_SOLANA_PROGRAM_ID=
VITE_FRUSD_MINT=
VITE_SOLSCAN_URL=https://solscan.io
